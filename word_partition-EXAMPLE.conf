{
    secrets  => [ $ENV{WORD_PARTITION_SECRET} // 'please-change-this-secret' ],
    database => {
        dsn      => $ENV{WORD_PARTITION_DSN}      // 'dbi:SQLite:dbname=/Users/you/Mojo-WordPart/word_partition.db',
        username => $ENV{WORD_PARTITION_DB_USER}  // 'root',
        password => $ENV{WORD_PARTITION_DB_PASS}  // '',
        # Kept for Lingua::Word::Parser, which wants these split out:
        database => $ENV{WORD_PARTITION_DB_NAME}  // '/Users/you/Mojo-WordPart/word_partition.db',
        driver   => $ENV{WORD_PARTITION_DB_DRIVER} // 'SQLite',
        host     => $ENV{WORD_PARTITION_DB_HOST}  // 'localhost',
    },
};
